import {Tables} from "asset/components/styleComponents";

export const Table = ({componentHead , componentBody}) => {

    return (
        <Tables>
            {componentHead}
            {componentBody}
        </Tables>
    )
}
