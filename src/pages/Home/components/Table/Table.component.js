import {Tables} from "asset/components/components";

export const Table = ({componentHead , componentBody}) => {

    return (
        <Tables>
            {componentHead}
            {componentBody}
        </Tables>
    )
}
